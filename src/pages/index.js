import { Container, Typography} from '@mui/material';

export default function Server() {
 
  return (
    <Container sx={{ height: '100vh' }}>
      <Typography>This Acts As A Server</Typography>
    </Container>
  );
}
